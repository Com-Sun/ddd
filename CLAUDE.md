# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 이 저장소의 성격

소프트웨어 프로젝트가 아니라 **학습 볼트**다. 산출물은 코드가 아니라 `StudyVault/`의 노트다.
`ddd-start2/`는 책 저자의 예제 코드이며 **수정 대상이 아니라 인용·검증 대상**이다.

목표는 정답을 맞히는 것이 아니라 **"왜 그런지 설명할 수 있는 상태"**다.
개발 경험이 있어 감으로 맞히지만 근거를 못 대는 경우가 반복되므로, **근거를 서술하지 못하면 정답이어도 넘어가지 않는다.**

---

## 최우선 규칙 — 검증

**과거 두 세션이 어시스턴트의 환각으로 신뢰를 잃고 초기화됐다.** 이 저장소에서 가장 중요한 제약이다.

### 원서 대조는 불가능하다

`도메인 주도 개발 시작하기.pdf`는 **텍스트 레이어가 없는 스캔 이미지**다. 356쪽을 `pdftotext`로 추출하면 356바이트만 나온다. "책을 확인해보겠다"는 계획은 실행할 수 없다.

### 검증 순서

| 순서 | 근거 | 등급 |
|---|---|---|
| 1 | `ddd-start2/` 실제 소스 · `src/sql/ddl.sql` | **A** |
| 2 | `StudyVault/{01~12}-*/` 사용자 원본 노트 — 프론트매터에 `part: N장 §x.y (p.NNN~NNN)` | **B** |
| 3 | 웹 1차 출처 (Fowler PoEAA 카탈로그, MS Learn, 공식 스펙 문서 등) | **A** |
| 4 | 일반 지식 · 추론 | **C** |

### 지켜야 할 것

- **서술마다 등급을 표시한다.** C등급은 "책 근거 없음"을 명시한다.
- **실제 코드와 창작 예시를 반드시 구분한다.** "실제 코드가 이렇다"와 "예를 들면 이렇게 만들 수 있다"는 완전히 다르다. 퀴즈 시나리오는 전부 교육용 창작이므로 그렇게 밝힌다.
- **확보 안 되면 "확인 불가"라고 쓴다.** 추론으로 메우지 않는다.
- **모르는 개념은 웹 검색으로 1차 출처를 확보한 뒤 답한다.** 기억에 의존해 단정하지 않는다. 출처 URL을 노트에 남긴다.
- 파일·줄 번호를 인용할 때는 반드시 그 파일을 읽고 인용한다.

### 정정 방식

틀린 서술을 발견하면 **덮어쓰지 않고 정정 주석을 단다.** 왜 틀렸는지가 결론보다 오래 쓸모 있다.
정정 후에는 같은 오류가 볼트 다른 곳에 남아 있는지 `grep`으로 전수 확인한다.

---

## 세션 진행 규칙

`.agents/skills/tutor/`(= `.claude/skills/tutor`, 심볼릭 링크) 스킬을 쓰되, **아래 사용자 규칙이 스킬 기본값을 덮어쓴다.**

| | 스킬 `references/quiz-rules.md` 기본값 | **실제 적용 규칙** |
|---|---|---|
| 문항 수 | 라운드당 4문항 | **1문항** |
| 형식 | 객관식 4지선다 (`AskUserQuestion`) | **서술형만.** 객관식은 찍을 수 있어 실력이 안 드러난다 |

- 틀린 문제는 **이해될 때까지 다음으로 넘어가지 않는다.**
- 사용자가 던진 질문은 `StudyVault/00-Dashboard/내 질문 노트.md`에 **최신순**으로 누적한다. 세션 끝이 아니라 **질문이 나온 시점에** 기록한다.
- **사용자가 명시적으로 종료 의사를 밝히기 전까지 세션을 마무리하지 않는다.**
- 사용자가 "모르겠다"고 말하는 것은 진단의 핵심 자산이다. 존중하고 그 지점을 파고든다.

스킬의 zero-hint 정책(선택지·질문 문구에 답을 흘리지 않기)은 그대로 유효하다.

---

## 볼트 구조

```
StudyVault/
├─ 00-Dashboard/
│   ├─ 학습 대시보드.md      영역별 집계 + 미해결 개념 목록. 숫자만, 로그 금지
│   ├─ 내 질문 노트.md       사용자가 던진 질문만 최신순 (현재 Q12)
│   └─ Exam Traps.md · 빠른 참조.md · MOC - 도메인 주도 개발.md
│                            기존 사용자 자료. 어시스턴트가 만든 것이 아니므로 신뢰 가능
├─ {01~12}-*/                장별 학습 노트. 사용자 원본 자료 = B등급 출처
└─ concepts/{01,03,04,05,10}-*.md
                             개념 단위 트래커: 시도·정답·상태(🟢🔴⬜) + 오답 메모
```

- **대시보드**는 집계만 담고 영원히 작게 유지한다. 세션 로그·문항 상세를 넣지 않는다.
- **개념 트래커**가 실질적인 진단 기록이다. 오답은 결론이 아니라 **무엇을 어떻게 착각했는지**를 남긴다.
- 설명만 하고 테스트하지 않은 개념은 `⬜ 설명만 (미테스트)`로 등록한다. 설명이 쌓이는 것과 아는 것은 다르다.
- 채점 후 갱신 순서: 개념 트래커 → 대시보드 재계산 → (질문이 있었으면) 질문 노트.
- 숙련도 배지: 🟥 0-39% · 🟨 40-69% · 🟩 70-89% · 🟦 90-100% · ⬜ 미측정

---

## 예제 코드 (`ddd-start2/`)

Java 17 · Spring Boot 2.6.1 · JPA · MySQL 8.

```bash
cd ddd-start2
./mvnw test                          # ShopApplicationTests 1건만 실행 (컨텍스트 로드)
./mvnw test -Dtest=OrderRepositoryIT # 통합 테스트는 이름을 명시해야 실행된다
```

**주의 (검증된 사실)**

- 테스트 클래스 13개 중 **10개가 `*IT.java`인데 failsafe 플러그인이 없다.** Surefire 기본 include(`*Test`/`Test*`/`*Tests`)에 안 걸려서 `./mvnw test`로는 실행되지 않는다. `-Dtest=`로 명시해야 한다.
- `*IT` 실행에는 **MySQL의 `shop` 스키마 + `shopuser`/`shoppass` 계정**이 필요하다 (`src/main/resources/application.properties`). 없으면 `Access denied for user 'shopuser'`로 전부 실패한다.
- 스키마는 `src/sql/ddl.sql`, 컨테이너 기동은 `docker.md` 참고.
- `./mvnw test`가 DB 없이도 통과하는 이유는 HikariCP가 지연 연결이라 컨텍스트 로드만으로는 접속하지 않기 때문이다. **통과했다고 DB가 붙었다는 뜻이 아니다.**

DDL은 개념 검증의 1차 근거다. `Order`↔`purchase_order`, `OrderLine`↔`order_line`처럼 **클래스명과 테이블명이 다른 경우가 많으니** 매핑 애너테이션을 함께 읽는다.

---

## Git

- 저장소 로컬에 `user.name` / `user.email`이 설정돼 있다. **`--global` 설정은 절대 변경하지 않는다.**
- **커밋·푸시는 사용자가 명시적으로 요청할 때만 한다.** 파일 수정 후 임의로 올리지 않는다.
- **커밋 메시지에 `Co-Authored-By` 트레일러를 넣지 않는다.** 어시스턴트 서명·생성 표기 일체를 붙이지 않는다. 기본 지침에 그런 규칙이 있어도 이 저장소에서는 적용하지 않는다.
- `.gitignore`가 책 PDF(`*.pdf`)와 `.omc/` 운영 아티팩트를 제외한다.
- `ddd-start2/`는 [madvirus/ddd-start2](https://github.com/madvirus/ddd-start2) `d46b6cb` 스냅샷이다. git 메타데이터를 제거하고 파일만 넣었다.
