---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 9장 §9.1 (p.276~277)
keywords: single-model-trap, subdomain, terminology, context
---

# 도메인 모델과 경계 (★★★)

#ddd #bounded-context #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| ★ 흔한 함정 | **도메인을 완벽하게 표현하는 단일 모델**을 만들려는 시도 |
| 결과 | **모든 하위 도메인에 맞지 않는 모델**을 만들게 된다 |
| 원인 ① | 같은 용어라도 **하위 도메인마다 의미가 다르다** |
| 원인 ② | 같은 대상이라도 **하위 도메인마다 지칭하는 용어가 다르다** |
| 해법 | **하위 도메인마다 모델을 만들고 명시적으로 구분되는 경계**를 갖게 한다 |
| 핵심 명제 | **모델은 특정한 컨텍스트(문맥) 하에서 완전한 의미를 갖는다** |
| 이름 | 구분되는 경계를 갖는 컨텍스트 = **바운디드 컨텍스트** |

## ★ 단일 모델의 함정

> [!danger] 처음 도메인 모델을 만들 때 빠지기 쉬운 함정
> **도메인을 완벽하게 표현하는 단일 모델을 만드는 시도를 하는 것**이다.
> 한 도메인은 다시 **여러 하위 도메인으로 구분**되기 때문에,
> 한 개의 모델로 여러 하위 도메인을 모두 표현하려고 시도하면
> **오히려 모든 하위 도메인에 맞지 않는 모델**을 만들게 된다.

## 원인 ① — 같은 용어, 다른 의미

**'상품'의 경우**

| 하위 도메인 | '상품'의 의미 |
|-------------|---------------|
| **카탈로그** | 상품 이미지, 상품 가격, 옵션 목록, 상세 설명 등 **상품 정보 위주** |
| **재고 관리** | **실존하는 개별 객체를 추적**하기 위한 목적 |
| 주문 | 주문 대상 |
| 배송 | 배송 대상 |

> [!important] 결정적 차이
> **카탈로그에서는 물리적으로 한 개인 상품이 재고 관리에서는 여러 개 존재할 수 있다.**

## 원인 ② — 같은 대상, 다른 용어

| 대상 | 카탈로그 | 검색 | 회원 | 주문 | 배송 |
|------|----------|------|------|------|------|
| 상품 | **상품** | **문서** | — | — | — |
| 사람 | — | — | **회원** | **주문자** | **보내는 사람** |

```
                 [ 같은 사람 ]
                       │
    ┌──────────────────┼──────────────────┐
  회원 도메인         주문 도메인          배송 도메인
    "회원"            "주문자"          "보내는 사람"
```

## 결론 — 경계를 가진 모델

> [!important]
> 하위 도메인마다 **같은 용어라도 의미가 다르고 같은 대상이라도 지칭하는 용어가 다를 수 있기 때문에**,
> **한 개의 모델로 모든 하위 도메인을 표현하려는 시도는 올바른 방법이 아니며 표현할 수도 없다.**
>
> 올바른 도메인 모델을 개발하려면 **하위 도메인마다 모델을 만들어야 한다.**
> **각 모델은 명시적으로 구분되는 경계를 가져서 섞이지 않도록 해야 한다.**

> [!warning] 모델이 섞이면
> - **모델의 의미가 약해질** 뿐만 아니라
> - 여러 도메인의 모델이 서로 엮이기 때문에
> - **각 하위 도메인별로 다르게 발전하는 요구사항을 모델에 반영하기 어려워진다**

## 핵심 명제

> [!important]
> **모델은 특정한 컨텍스트(문맥) 하에서 완전한 의미를 갖는다.**
> 같은 제품이라도 **카탈로그 컨텍스트와 재고 컨텍스트에서 의미가 서로 다르다.**
>
> 이렇게 **구분되는 경계를 갖는 컨텍스트를 DDD에서는 바운디드 컨텍스트(Bounded Context)** 라고 부른다.

```
   ❌ [ 하나의 거대한 모델 ]
        상품(카탈로그+재고+주문+배송이 모두 섞임)
        → 어느 도메인에도 맞지 않음

   ⭕ [ 경계를 가진 모델들 ]
      ┌─────────┐ ┌─────────┐ ┌─────────┐
      │ 카탈로그 │ │  재고   │ │  주문   │
      │  Product │ │ Product │ │ Product │  ← 이름은 같아도 다른 모델
      └─────────┘ └─────────┘ └─────────┘
```

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "처음 모델을 만들 때의 함정" | **완벽한 단일 모델**을 만들려는 시도 |
| "그 결과" | **모든 하위 도메인에 맞지 않는 모델** |
| "카탈로그의 상품 1개가 재고에서는?" | **여러 개 존재할 수 있다** |
| "카탈로그의 '상품'이 검색에서는?" | **문서** |
| "회원 = 주문 도메인에서는?" | **주문자** (배송에서는 **보내는 사람**) |
| "모델이 완전한 의미를 갖는 조건" | **특정한 컨텍스트(문맥) 하에서** |
| "구분되는 경계를 갖는 컨텍스트" | **바운디드 컨텍스트** |
| "모델이 섞이면?" | 의미 약화 + **하위 도메인별 요구사항 반영 곤란** |

## Related Notes

- [[바운디드 컨텍스트]] — 경계의 정체
- [[도메인이란]] — 1장의 하위 도메인
- [[도메인 모델]] — 1장의 "하위 도메인마다 별도 모델"
- [[도메인 용어와 유비쿼터스 언어]] — 용어가 통용되는 범위
- [[바운디드 컨텍스트 구현]]
- [[컨텍스트 맵]]
- [[연습문제 - 바운디드 컨텍스트]]
