---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 6장 §6.5 (p.221~226)
keywords: validation, errors, validator, exception, user-experience
---

# 값 검증 (★★★)

#ddd #application-service #validation #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 수행 가능 위치 | **표현 영역**과 **응용 서비스** 두 곳 모두 |
| **원칙** | **모든 값에 대한 검증은 응용 서비스에서 처리** |
| 표현 영역의 필요 | 잘못된 값을 **사용자에게 알려주고 다시 입력**받아야 한다 |
| ★ 익셉션 방식의 문제 | 첫 번째 오류에서 멈춰 **나머지 항목 오류를 알 수 없다** → 나쁜 UX |
| 해법 A | 응용 서비스가 **에러 코드를 모아 하나의 익셉션**으로 발생 |
| 해법 B | **표현 영역에서 필수 값·형식 검증**, 응용 서비스는 **논리적 오류** 검증 |
| 저자의 현재 입장 | **응용 서비스에서 필수 값 검증과 논리적 오류를 모두** 하는 편 |

## 원칙 — 응용 서비스에서 검증

```java
public class JoinService {

    @Transactional
    public void join(JoinRequest joinReq) {
        // 값의 형식 검사
        checkEmpty(joinReq.getId(), "id");
        checkEmpty(joinReq.getName(), "name");
        checkEmpty(joinReq.getPassword(), "password");
        if (!joinReq.getPassword().equals(joinReq.getConfirmPassword()))
            throw new InvalidPropertyException("confirmPassword");

        // 로직 검사
        checkDuplicateId(joinReq.getId());
        ...
    }

    private void checkEmpty(String value, String propertyName) {
        if (value == null || value.isEmpty())
            throw new EmptyPropertyException(propertyName);
    }

    private void checkDuplicateId(String id) {
        int count = memberRepository.countById(id);
        if (count > 0) throw new DuplicateIdException();
    }
}
```

## ★ 익셉션 방식의 문제 — 나쁜 사용자 경험

> [!danger] 무엇이 문제인가 (p.222)
> 사용자는 폼에 값을 입력하고 전송했는데 입력한 값이 잘못되어 다시 입력해야 할 때,
> **한 개 항목이 아닌 입력한 모든 항목에 대해 잘못된 값이 존재하는지 알고 싶을 것**이다.
> 그래야 **한 번에 잘못된 값을 제대로 입력**할 수 있기 때문이다.
>
> **그런데 응용 서비스에서 첫 번째 값이 올바르지 않아 익셉션을 발생시키면 나머지 항목은 검사하지 않게 된다.**
> → 사용자는 **첫 번째 값에 대한 에러 메시지만 보게 되고**, 결과적으로 **같은 폼에 값을 여러 번 입력하게 된다.**

```
   [ 익셉션 즉시 발생 ]
   id 비었음 → throw  →  name, password 오류는 알 수 없음
        ▼
   사용자: id 고침 → 전송 → "name도 비었네요" → ...  ❌ 반복
```

### 컨트롤러가 번잡해진다

```java
@Controller
public class Controller {
    @PostMapping("/member/join")
    public String join(JoinRequest joinRequest, Errors errors) {
        try {
            joinService.join(joinRequest);
            return successView;
        } catch (EmptyPropertyException ex) {
            // 표현 영역은 잘못 입력한 값이 존재하면 이를 사용자에게 알려주고
            // 폼을 다시 입력할 수 있도록 하기 위해 관련 기능 사용
            errors.rejectValue(ex.getPropertyName(), "empty");
            return formView;
        } catch (InvalidPropertyException ex) {
            errors.rejectValue(ex.getPropertyName(), "invalid");
            return formView;
        } catch (DuplicateIdException ex) {
            errors.rejectValue(ex.getPropertyName(), "duplicate");
            return formView;
        }
    }
}
```

## ⭕ 해법 A — 에러를 모아 하나의 익셉션으로

```java
@Transactional
public OrderNo placeOrder(OrderRequest orderRequest) {
    List<ValidationError> errors = new ArrayList<>();
    if (orderRequest == null) {
        errors.add(ValidationError.of("empty"));
    } else {
        if (orderRequest.getOrdererMemberId() == null)
            errors.add(ValidationError.of("ordererMemberId", "empty"));
        if (orderRequest.getOrderProducts() == null)
            errors.add(ValidationError.of("orderProducts", "empty"));
        if (orderRequest.getOrderProducts().isEmpty())
            errors.add(ValidationError.of("orderProducts", "empty"));
    }
    // 응용 서비스가 입력 오류를 하나의 익셉션으로 모아서 발생
    if (!errors.isEmpty()) throw new ValidationErrorException(errors);
    ...
}
```

표현 영역은 익셉션에서 **에러 목록을 가져와 표현 영역에서 사용할 형태로 변환**한다.

```java
@PostMapping("/orders/order")
public String order(@ModelAttribute("orderReq") OrderRequest orderRequest,
                    BindingResult bindingResult,
                    ModelMap modelMap) {
    User user = (User) SecurityContextHolder.getContext()
            .getAuthentication().getPrincipal();
    orderRequest.setOrdererMemberId(MemberId.of(user.getUsername()));
    try {
        OrderNo orderNo = placeOrderService.placeOrder(orderRequest);
        modelMap.addAttribute("orderNo", orderNo.getNumber());
    } catch (ValidationErrorException e) {
        // 응용 서비스가 발생시킨 검증 에러 목록을 뷰에서 사용할 형태로 변환
        e.getErrors().forEach(err -> {
            if (err.hasName()) {
                bindingResult.rejectValue(err.getName(), err.getCode());
            } else {
                bindingResult.reject(err.getCode());
            }
        });
        populateProductsModel(orderRequest, modelMap);
        return "order/confirm";
    }
}
```

## ⭕ 해법 B — 표현 영역에서 형식 검증

```java
@Controller
public class Controller {
    @PostMapping("/member/join")
    public String join(JoinRequest joinRequest, Errors errors) {
        checkEmpty(joinRequest.getId(), "id", errors);
        checkEmpty(joinRequest.getName(), "name", errors);
        ... // 나머지 값 검증

        // 모든 값의 형식을 검증한 뒤, 에러 존재하면 다시 폼을 보여줌
        if (errors.hasErrors()) return formView;

        try {
            joinService.join(joinRequest);
            return successView;
        } catch (DuplicateIdException ex) {
            errors.rejectValue(ex.getPropertyName(), "duplicate");
            return formView;
        }
    }

    private void checkEmpty(String value, String property, Errors errors) {
        if (isEmpty(value)) errors.rejectValue(property, "empty");
    }
}
```

### `Validator` 인터페이스로 더 간결하게

> 스프링 같은 프레임워크는 값 검증을 위한 **`Validator` 인터페이스**를 제공하므로, 검증기를 따로 구현하면 코드를 간결하게 줄일 수 있다.

```java
@Controller
public class Controller {
    @PostMapping("/member/join")
    public String join(JoinRequest joinRequest, Errors errors) {
        new JoinRequestValidator().validate(joinRequest, errors);
        if (errors.hasErrors()) return formView;

        try {
            joinService.join(joinRequest);
            return successView;
        } catch (DuplicateIdException ex) {
            errors.rejectValue(ex.getPropertyName(), "duplicate");
            return formView;
        }
    }
}
```

> [!tip] 역할 분담
> 표현 영역에서 필수 값과 값의 형식을 검사하면, **실질적으로 응용 서비스는 ID 중복 여부와 같은 논리적 오류만 검사**하면 된다.
> 즉 **표현 영역과 응용 서비스가 값 검사를 나눠서 수행**하는 것이다.

## ★ 저자의 입장 변화 (p.225~226)

| 시기 | 입장 |
|------|------|
| 예전 | **표현 영역에서 필수 값 검증**, 응용 서비스에서는 **논리적 오류 검증** |
| 요즘 | 가능하면 **응용 서비스에서 필수 값 검증과 논리적 오류를 모두** 하는 편 |

| 응용 서비스에서 모두 검증할 때 | 평가 |
|--------------------------------|------|
| 단점 | 프레임워크가 제공하는 검증 기능을 쓸 때보다 **작성할 코드가 늘어난다** |
| 장점 | **응용 서비스의 완성도가 높아진다** |

> 저자는 **이 이점이 더 크게 느껴져 응용 서비스에서 값 오류를 검증하는 편**이다.

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "값 검증은 어디서 할 수 있나?" | **표현 영역 + 응용 서비스** 두 곳 |
| "원칙적으로 모든 값 검증은?" | **응용 서비스** |
| "익셉션 즉시 발생 방식의 문제" | **첫 오류만 알려줘 나쁜 UX** (여러 번 입력) |
| "해법 A" | 에러 코드를 모아 **하나의 익셉션**(`ValidationErrorException`) |
| "해법 B" | **표현 영역에서 형식 검증**, 응용 서비스는 논리적 오류 |
| "스프링이 제공하는 검증 인터페이스" | **`Validator`** |
| "저자의 현재 입장" | **응용 서비스에서 필수 값 + 논리적 오류 모두 검증** |
| "그 이유" | 코드는 늘지만 **응용 서비스의 완성도가 높아진다** |

## Related Notes

- [[표현 영역]] — 표현 영역의 책임
- [[표현 영역과 응용 영역]]
- [[응용 서비스의 역할]]
- [[요청 처리 흐름]] — 2장의 형식 검사
- [[도메인 모델 도출]] — 1장의 생성자 검증
- [[권한 검사]]
- [[연습문제 - 응용 서비스와 표현 영역]]
