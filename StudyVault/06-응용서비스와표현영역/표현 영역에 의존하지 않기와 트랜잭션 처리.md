---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 6장 §6.3.4~6.3.5 (p.215~217)
keywords: decoupling, httpservletrequest, session, transactional, rollback
---

# 표현 영역에 의존하지 않기 / 트랜잭션 처리 (★★★)

#ddd #application-service #transaction #concept

## Overview Table (한눈에 비교)

| 주제 | 핵심 |
|------|------|
| **금지** | 응용 서비스의 **파라미터·리턴 타입에 표현 영역의 구현 기술** 사용 |
| 금지 대상 예 | `HttpServletRequest`, `HttpSession`, 쿠키 |
| 문제 ① | 응용 서비스만 **단독으로 테스트하기 어려워진다** |
| 문제 ② | **표현 영역 구현이 변경되면 응용 서비스도 변경**해야 한다 |
| **문제 ③ (가장 심각)** | 응용 서비스가 **표현 영역의 역할까지 대신**하게 된다 → 표현 영역 응집도 붕괴 |
| 지키는 가장 쉬운 방법 | **파라미터와 리턴 타입에 표현 영역 기술을 쓰지 않는다** |
| **트랜잭션** | **프레임워크가 제공하는 기능을 적극 사용** (`@Transactional`) |

## §6.3.4 표현 영역에 의존하지 않기

### ❌ 나쁜 예

```java
@Controller
@RequestMapping("/member/changePassword")
public class MemberPasswordController {

    @PostMapping
    public String submit(HttpServletRequest request) {
        try {
            // ❌ 응용 서비스가 표현 영역을 의존하면 안 됨!
            changePasswordService.changePassword(request);
        } catch (NoMemberException ex) {
            // 알맞은 익셉션 처리 및 응답
        }
    }
}
```

### 세 가지 문제

| # | 문제 |
|---|------|
| ① | 응용 서비스에서 표현 영역에 대한 의존이 발생하면 **응용 서비스만 단독으로 테스트하기가 어려워진다** |
| ② | **표현 영역의 구현이 변경되면 응용 서비스의 구현도 함께 변경**해야 한다 |
| ③ | **(가장 심각)** 응용 서비스가 **표현 영역의 역할까지 대신하는 상황**이 벌어질 수 있다 |

### ★ 가장 심각한 문제 — 표현 영역 역할 침범

```java
public class AuthenticationService {

    public void authenticate(HttpServletRequest request) {
        String id = request.getParameter("id");
        String password = request.getParameter("password");

        if (checkIdPasswordMatching(id, password)) {
            // ❌ 응용 서비스에서 표현 영역의 상태 처리
            HttpSession session = request.getSession();
            session.setAttribute("auth", new Authentication(id));
        }
    }
}
```

> [!danger]
> **`HttpSession`이나 쿠키는 표현 영역의 상태**에 해당하는데,
> 이 상태를 응용 서비스에서 변경해 버리면 **표현 영역의 코드만으로 표현 영역의 상태가 어떻게 변경되는지 추적하기 어려워진다.**
> 즉 **표현 영역의 응집도가 깨진다.** 이것은 결과적으로 **코드 유지 보수 비용을 증가**시킨다.

```
   [ 정상 ]  세션 상태 변경 → 표현 영역 코드에만 존재 → 추적 쉬움
   [ 위반 ]  세션 상태 변경 → 응용 서비스에도 존재   → 어디서 바뀌는지 추적 불가
```

> [!important] 지키는 방법
> 앞서 언급한 문제가 발생하지 않도록 하려면 **철저하게 응용 서비스가 표현 영역의 기술을 사용하지 않도록** 해야 한다.
> 이를 지키기 위한 **가장 쉬운 방법이 서비스 메서드의 파라미터와 리턴 타입으로 표현 영역의 구현 기술을 사용하지 않는 것**이다.

## §6.3.5 트랜잭션 처리

> 회원 가입에 성공했다고 하면서 실제로는 가입되지 않았다면 곤란하다.
> 비슷하게 배송지 주소를 변경하는 데 실패했다는 안내 화면을 보여줬는데 실제로는 변경되었다면 역시 곤란하다.
> **트랜잭션 관리는 응용 서비스의 중요한 역할**이다.

```java
public class ChangePasswordService {

    @Transactional
    public void changePassword(ChangePasswordRequest req) {
        Member member = findExistingMember(req.getMemberId());
        member.changePassword(req.getCurrentPassword(), req.getNewPassword());
    }
}
```

> [!important] 프레임워크 기능을 적극 사용하라
> **프레임워크가 제공하는 트랜잭션 기능을 적극 사용하는 것이 좋다.**
> 프레임워크가 제공하는 규칙을 따르면 **간단한 설정만으로 트랜잭션을 시작하여 커밋하고 롤백**할 수 있다.
>
> 스프링은 **`@Transactional`이 적용된 메서드가 `RuntimeException`을 발생시키면 트랜잭션을 롤백**하고,
> 그렇지 않으면 커밋한다. → **트랜잭션 처리 코드를 간결하게 유지**할 수 있다.

```
   @Transactional 메서드
        ├─ RuntimeException 발생  →  ROLLBACK
        └─ 정상 종료               →  COMMIT
```

> [!note] 왜 프레임워크를 쓰는가
> 2장 [[인프라스트럭처 개요]]에서 본 것처럼, `@Transactional` 의존을 없애려면 복잡한 설정이 필요한데
> 그렇게 해도 **테스트가 더 쉬워지거나 유연함이 증가하지 않는다.** 이는 합리적인 타협이다.

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "응용 서비스 파라미터로 `HttpServletRequest`" | ❌ **표현 영역 의존 금지** |
| "표현 영역 의존의 세 문제" | **단독 테스트 어려움 / 변경 전파 / 표현 영역 역할 침범** |
| "가장 심각한 문제" | 응용 서비스가 **세션·쿠키 등 표현 영역 상태를 조작** |
| "그 결과" | **표현 영역의 응집도가 깨진다** → 유지 보수 비용 ↑ |
| "지키는 가장 쉬운 방법" | **파라미터·리턴 타입에 표현 영역 기술 사용 금지** |
| "트랜잭션 관리 주체" | **응용 서비스** |
| "스프링에서 롤백 조건" | **`RuntimeException` 발생 시** |

## Related Notes

- [[표현 영역과 응용 영역]]
- [[메서드 파라미터와 값 리턴]]
- [[표현 영역]] — 세션 관리는 표현 영역의 역할
- [[요청 처리 흐름]] — 2장
- [[인프라스트럭처 개요]] — `@Transactional` 타협 논의
- [[권한 검사]]
- [[연습문제 - 응용 서비스와 표현 영역]]
