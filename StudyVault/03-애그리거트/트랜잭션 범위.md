---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 3장 §3.2.3 (p.109~111)
keywords: transaction-scope, locking, throughput, coupling, domain-event
---

# 트랜잭션 범위 (★★★)

#ddd #aggregate #transaction #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| **대원칙** | **트랜잭션 범위는 작을수록 좋다** |
| 애그리거트 원칙 | **한 트랜잭션에서는 한 애그리거트만 수정**한다 |
| 이유 ① | 잠금 대상이 늘면 **동시 처리 트랜잭션 수가 줄어** 전체 처리량이 떨어진다 |
| 이유 ② | 애그리거트 간 **결합도가 높아져** 수정 비용이 증가한다 |
| 부득이할 때 | 애그리거트가 아닌 **응용 서비스에서** 두 애그리거트를 수정한다 |
| 더 나은 방법 | **도메인 이벤트** (10장) |
| 예외 3가지 | **팀 표준 / 기술 제약 / UI 구현의 편리** |

## 왜 작을수록 좋은가 — 성능

> [!important]
> 한 트랜잭션이 **한 개 테이블**을 수정하는 것과 **세 개의 테이블**을 수정하는 것을 비교하면 성능에서 차이가 발생한다.

```
  한 테이블 수정  →  잠금 대상: 한 테이블의 한 행
  세 테이블 수정  →  잠금 대상: 더 많아짐
                          ▼
              동시에 처리할 수 있는 트랜잭션 개수 ↓
                          ▼
                전체적인 성능(처리량) ↓
```

동일하게 **한 트랜잭션에서 두 개 이상의 애그리거트를 수정하면 트랜잭션 충돌이 발생할 가능성이 더 높아지므로**, 한 번에 수정하는 애그리거트 개수가 많아질수록 **전체 처리량이 떨어진다.**

## 한 트랜잭션 = 한 애그리거트

> [!danger] 애그리거트에서 다른 애그리거트를 변경하지 마라
> 한 트랜잭션에서 한 애그리거트만 수정한다는 것은
> **애그리거트에서 다른 애그리거트를 변경하지 않는다**는 것을 의미한다.
> 한 애그리거트에서 다른 애그리거트를 수정하면 결과적으로 **두 애그리거트를 한 트랜잭션에서 수정**하게 되므로,
> **애그리거트 내부에서 다른 애그리거트의 상태를 변경하면 안 된다.**

### ❌ 나쁜 예

```java
public class Order {
    private Orderer orderer;

    public void shipTo(ShippingInfo newShippingInfo,
                       boolean useNewShippingAddrAsMemberAddr) {
        verifyNotYetShipped();
        setShippingInfo(newShippingInfo);
        if (useNewShippingAddrAsMemberAddr) {
            // ❌ 다른 애그리거트의 상태를 변경하면 안 됨!
            orderer.getMember().changeAddress(newShippingInfo.getAddress());
        }
    }
}
```

> [!warning] 무엇이 문제인가
> 이것은 **애그리거트가 자신의 책임 범위를 넘어 다른 애그리거트의 상태까지 관리하는 꼴**이 된다.
> 애그리거트는 **최대한 서로 독립적이어야 하는데**, 한 애그리거트가 다른 애그리거트의 기능에 의존하기 시작하면 **애그리거트 간 결합도가 높아진다.**
> 결합도가 높아질수록 **향후 수정 비용이 증가**한다.

### ⭕ 응용 서비스에서 조합하라

```java
public class ChangeOrderService {

    // 두 개 이상의 애그리거트를 변경해야 하면,
    // 응용 서비스에서 각 애그리거트의 상태를 변경한다.
    @Transactional
    public void changeShippingInfo(OrderId id, ShippingInfo newShippingInfo,
                                   boolean useNewShippingAddrAsMemberAddr) {
        Order order = orderRepository.findById(id);
        if (order == null) throw new OrderNotFoundException();
        order.shipTo(newShippingInfo);

        if (useNewShippingAddrAsMemberAddr) {
            Member member = findMember(order.getOrderer());
            member.changeAddress(newShippingInfo.getAddress());
        }
    }
}
```

```
   ❌ Order ──직접 수정──▶ Member          (애그리거트 간 결합)
   ⭕ ApplicationService ──▶ Order
                        └──▶ Member       (응용 서비스가 조합)
```

## 더 나은 방법 — 도메인 이벤트

> [!tip]
> **도메인 이벤트**를 사용하면 한 트랜잭션에서 **한 개의 애그리거트를 수정하면서도**
> **동기나 비동기로 다른 애그리거트의 상태를 변경**하는 코드를 작성할 수 있다.
> → 자세한 내용은 [[이벤트 개요]] (10장) 참고.

## ★ 예외 — 두 개 이상을 변경해도 되는 경우

한 트랜잭션에서 한 개의 애그리거트를 변경하는 것을 권장하지만, 다음 경우에는 **두 개 이상의 애그리거트 변경을 고려할 수 있다.**

| 예외 | 설명 |
|------|------|
| **① 팀 표준** | 팀이나 조직의 표준에 따라 사용자 유스케이스와 관련된 응용 서비스의 기능을 **한 트랜잭션으로 실행해야 하는 경우**가 있다 |
| **② 기술 제약** | 기술적으로 **이벤트 방식을 도입할 수 없는 경우**, 한 트랜잭션에서 다수의 애그리거트를 수정해서 일관성을 처리해야 한다 |
| **③ UI 구현의 편리** | 운영자의 편리함을 위해 **주문 목록 화면에서 여러 주문의 상태를 한 번에 변경**하고 싶을 때. 이 경우 한 트랜잭션에서 여러 주문 애그리거트의 상태를 변경해야 한다 |

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "트랜잭션 범위의 대원칙" | **작을수록 좋다** |
| "한 트랜잭션에서 몇 개의 애그리거트?" | **한 개** |
| "잠금 대상이 많아지면?" | 동시 처리 트랜잭션 ↓ → **처리량 ↓** |
| "`order.shipTo()`에서 member 주소를 바꾸면?" | ❌ **애그리거트 간 결합도 상승** |
| "부득이하게 둘을 바꿔야 하면?" | **응용 서비스에서** 각각 변경 |
| "한 트랜잭션 한 애그리거트를 지키며 다른 애그리거트를 바꾸려면?" | **도메인 이벤트** |
| "두 개 이상 변경 허용 예외 3가지" | **팀 표준 / 기술 제약 / UI 구현의 편리** |

## Related Notes

- [[애그리거트 루트]] — 일관성 유지 주체
- [[애그리거트]] — 애그리거트는 자기 자신만 관리
- [[ID를 이용한 애그리거트 참조]] — 결합도를 낮추는 방법
- [[애그리거트와 트랜잭션]] — 8장의 동시성 문제
- [[이벤트 개요]] — 10장의 해법
- [[응용 서비스의 역할]] — 6장
- [[연습문제 - 애그리거트]]
