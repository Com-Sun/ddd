---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 3장 §3.2~3.2.2 (p.103~109)
keywords: aggregate-root, consistency, setter, immutable, delegation
---

# 애그리거트 루트 (★★★)

#ddd #aggregate #aggregate-root #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 정의 | 애그리거트 **전체를 관리할 주체**가 되는 **대표 엔티티** |
| 핵심 역할 | 애그리거트의 **일관성이 깨지지 않도록** 하는 것 |
| 방법 | 애그리거트가 제공해야 할 **도메인 기능을 루트가 구현**한다 |
| **금지** | 애그리거트 **외부에서 내부 객체를 직접 변경**하면 안 된다 |
| 습관 ① | 단순히 필드를 변경하는 **공개(public) set 메서드를 만들지 않는다** |
| 습관 ② | **밸류 타입은 불변**으로 구현한다 |
| 기능 구현 방식 | 내부 객체를 **조합**하거나, 내부 객체에 **기능 실행을 위임** |

## 왜 루트가 필요한가 — 일관성

주문 애그리거트는 다음을 포함한다.

- 총 금액인 `totalAmounts`를 갖고 있는 **`Order` 엔티티**
- 개별 구매 상품의 개수인 `quantity`와 금액인 `price`를 갖고 있는 **`OrderLine` 밸류**

> 도메인 규칙: **주문 총 금액은 개별 상품의 주문 개수 × 가격의 합이다.**

구매할 상품의 개수를 변경하면 `OrderLine`의 `quantity`를 변경하고, **더불어 `Order`의 `totalAmounts`도 변경해야 한다.** 그렇지 않으면 **데이터 일관성이 깨진다.**

> [!important]
> 애그리거트는 여러 객체로 구성되기 때문에 **한 객체만 상태가 정상이면 안 된다.**
> 도메인 규칙을 지키려면 **애그리거트에 속한 모든 객체가 정상 상태**를 가져야 한다.
>
> 이 책임을 지는 것이 바로 **애그리거트의 루트 엔티티**다.

```
              [ 주문 애그리거트 ]
        ┌──────────────────────────┐
        │   «루트» Order            │  ← 대표 엔티티
        │      ├── OrderLine        │
        │      ├── ShippingInfo     │
        │      │      ├── Address   │  ← 간접적으로 속함
        │      │      └── Receiver  │
        │      └── Orderer          │
        └──────────────────────────┘
   애그리거트에 속한 객체는 루트에 직접 또는 간접적으로 속한다
```

## §3.2.1 도메인 규칙과 일관성

> [!important] 루트의 핵심 역할
> 애그리거트 루트가 단순히 속한 객체를 **포함하는 것으로 끝나는 것은 아니다.**
> 핵심 역할은 **애그리거트의 일관성이 깨지지 않도록 하는 것**이며,
> 이를 위해 애그리거트 루트는 **애그리거트가 제공해야 할 도메인 기능을 구현**한다.

```java
public class Order {

    // 애그리거트 루트는 도메인 규칙을 구현한 기능을 제공한다.
    public void changeShippingInfo(ShippingInfo newShippingInfo) {
        verifyNotYetShipped();
        setShippingInfo(newShippingInfo);
    }

    private void verifyNotYetShipped() {
        if (!isNotYetShipped())
            throw new AlreadyShippedException();
    }

    public boolean isNotYetShipped() {
        return state == OrderState.PAYMENT_WAITING || state == OrderState.PREPARING;
    }
}
```

> [!note] 실제 예제 코드 기준으로 정정
> 책 본문은 `IllegalStateException("already shipped")`를 던지지만,
> **실제 `ddd-start2/`는 전용 익셉션 `AlreadyShippedException`** 을 사용하고
> 판단 로직을 **`public boolean isNotYetShipped()`** 로 분리해 두었다.
>
> 판단 메서드를 `public`으로 뺀 이유는 **표현 영역이 "취소 버튼을 보여줄지" 결정**할 때 쓰기 위함이다.
> 상태를 **바꾸는 것은 루트만**, **묻는 것은 외부에서도** — 캡슐화를 지키면서 화면 요구를 만족시키는 방식이다.
> → `order/command/domain/Order.java`, [[코드 대조 검증 보고서]]

### ❌ 외부에서 직접 변경하면 안 된다

```java
ShippingInfo si = order.getShippingInfo();
si.setAddress(newAddress);          // ❌ 루트를 우회
```

> [!danger] 무엇이 문제인가
> 주문 **상태에 상관없이** 배송지 주소를 변경한다.
> 이는 **업무 규칙을 무시하고 직접 DB 테이블의 데이터를 수정하는 것과 같은 결과**를 만든다.
> 즉 **논리적인 데이터 일관성이 깨진다.**

### ❌ 응용 서비스에서 검사하는 것도 답이 아니다

```java
ShippingInfo si = order.getShippingInfo();

// 주요 도메인 로직이 중복되는 문제
if (state != OrderState.PAYMENT_WAITING && state != OrderState.PREPARING) {
    throw new IllegalArgumentException();
}
si.setAddress(newAddress);
```

> [!warning]
> 이렇게 하면 **동일한 검사 로직을 여러 응용 서비스에서 중복으로 구현할 가능성**이 높아져 **유지 보수에 도움이 되지 않는다.**

### ⭕ 두 가지 습관

> [!important] 불필요한 중복을 피하고 루트를 통해서만 도메인 로직을 구현하게 만들려면
> ① **단순히 필드를 변경하는 set 메서드를 공개(public) 범위로 만들지 않는다.**
> ② **밸류 타입은 불변으로 구현한다.**

**습관 ① — 공개 set 메서드 금지**

```java
// ❌ 도메인 모델에서 공개 set 메서드는 가급적 피해야 한다.
public void setName(String name) {
    this.name = name;
}
```

> [!tip] 부가 효과
> 공개 set 메서드를 사용하지 않으면 **의미가 드러나는 메서드를 사용해서 구현할 가능성이 높아진다.**
> 예: `setState()` 대신 자연스럽게 `cancel()`, `changePassword()` 같은 이름을 쓰게 된다.

**습관 ② — 밸류는 불변으로**

```java
ShippingInfo si = order.getShippingInfo();
si.setAddress(newAddress);   // ShippingInfo가 불변이면, 이 코드는 컴파일 에러!
```

밸류 객체가 불변이면 값을 변경하는 방법은 **새로운 밸류 객체를 할당하는 것뿐**이다.

```java
public class Order {
    private ShippingInfo shippingInfo;

    public void changeShippingInfo(ShippingInfo newShippingInfo) {
        verifyNotYetShipped();
        setShippingInfo(newShippingInfo);
    }

    // set 메서드의 접근 허용 범위는 private이다.
    private void setShippingInfo(ShippingInfo newShippingInfo) {
        // 밸류가 불변이면 새로운 객체를 할당해서 값을 변경해야 한다.
        // 불변이므로 this.shippingInfo.setAddress(...)와 같은 코드를 사용할 수 없다.
        this.shippingInfo = newShippingInfo;
    }
}
```

> [!important] 결론
> 밸류 타입의 내부 상태를 변경하려면 **애그리거트 루트를 통해서만 가능**하다.
> 그러므로 **애그리거트 루트가 도메인 규칙을 올바르게만 구현하면 애그리거트 전체의 일관성을 올바르게 유지**할 수 있다.

## §3.2.2 애그리거트 루트의 기능 구현

### 방식 ① — 내부 객체를 조합

```java
public class Order {
    private Money totalAmounts;
    private List<OrderLine> orderLines;

    private void calculateTotalAmounts() {
        int sum = orderLines.stream()
                            .mapToInt(ol -> ol.getPrice() * ol.getQuantity())
                            .sum();
        this.totalAmounts = new Money(sum);
    }
}
```

```java
public class Member {
    private Password password;

    public void changePassword(String currentPassword, String newPassword) {
        if (!password.match(currentPassword)) {
            throw new PasswordNotMatchException();
        }
        this.password = new Password(newPassword);
    }
}
```

### 방식 ② — 기능 실행을 위임

구현 기술의 제약이나 내부 모델링 규칙 때문에 `OrderLine` 목록을 별도 클래스로 분리한 경우:

```java
public class OrderLines {
    private List<OrderLine> lines;

    public Money getTotalAmounts() { ...구현; }

    public void changeOrderLines(List<OrderLine> newLines) {
        this.lines = newLines;
    }
}

public class Order {
    private OrderLines orderLines;

    public void changeOrderLines(List<OrderLine> newLines) {
        orderLines.changeOrderLines(newLines);          // 위임
        this.totalAmounts = orderLines.getTotalAmounts();  // 총합 재계산
    }
}
```

> [!danger] 위임 시 반드시 막아야 할 버그
> `Order`가 `getOrderLines()` 같은 메서드로 `OrderLines`를 그대로 리턴하면
> **외부에서 애그리거트 내부 상태를 변경**할 수 있게 된다.
>
> ```java
> OrderLines lines = order.getOrderLines();
> lines.changeOrderLines(newOrderLines);   // ❌
> // Order의 totalAmounts 값이 OrderLines와 일치하지 않게 됨
> ```
>
> → **주문의 `OrderLine` 목록이 바뀌는데 총합은 계산하지 않는 버그**가 생긴다.

**해법 두 가지**

| 방법 | 설명 |
|------|------|
| ① **불변으로 구현** | 애초에 애그리거트 외부에서 `OrderLine` 목록을 변경할 수 없도록 `OrderLines`를 **불변**으로 만든다 |
| ② **접근 범위 제한** | 팀 표준이나 구현 기술 제약으로 불변이 불가능하면, 변경 기능을 **패키지나 protected 범위로 한정**해서 외부에서 실행할 수 없게 한다 |

> [!tip]
> 보통 **한 애그리거트에 속하는 모델은 한 패키지에 속하기 때문에**, 패키지나 `protected` 범위를 사용하면 **애그리거트 외부에서 상태 변경 기능을 실행하는 것을 방지**할 수 있다.

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "애그리거트 루트의 핵심 역할" | 애그리거트의 **일관성이 깨지지 않도록** 하는 것 |
| "`order.getShippingInfo().setAddress()`" | ❌ **DB를 직접 수정하는 것과 같은 결과** |
| "응용 서비스에서 상태를 검사하면?" | **도메인 로직 중복** → 유지 보수 악화 |
| "루트를 통하게 만드는 두 습관" | **공개 set 금지** + **밸류 불변** |
| "밸류가 불변이면 값 변경 방법" | **새로운 밸류 객체를 할당** |
| "set 메서드의 접근 범위" | **private** |
| "`OrderLines`를 그대로 리턴하면?" | 총합이 안 맞는 **버그** 발생 |
| "위임 시 방어 방법 2가지" | **불변** 또는 **패키지/protected 범위 제한** |

## Related Notes

- [[애그리거트]] — 애그리거트의 개념과 경계
- [[애그리거트 개요]] — 2장 소개
- [[트랜잭션 범위]]
- [[도메인 모델에 set 메서드 넣지 않기]] — 1장의 같은 원칙
- [[밸류 타입]] — 불변의 이유
- [[애그리거트를 팩토리로 사용하기]]
- [[연습문제 - 애그리거트]]
