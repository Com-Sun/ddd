---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 2장 §2.4.3 (p.086~089)
keywords: repository, persistence, aggregate-unit, dip, transaction
---

# 리포지터리 개요 (★★★)

#ddd #architecture #repository #dip #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 정의 | 도메인 객체를 **물리적인 저장소에 보관**하기 위한 도메인 모델 |
| 위상 | 엔티티·밸류가 **요구사항에서 도출되는** 도메인 모델이라면, 리포지터리는 **구현을 위한** 도메인 모델 |
| **저장/조회 단위** | **애그리거트 단위** |
| 인터페이스 위치 | **도메인 모델 영역** (고수준 모듈) |
| 구현 클래스 위치 | **인프라스트럭처 영역** (저수준 모듈) |
| 기본 메서드 2개 | ① 애그리거트를 **저장** ② 애그리거트 루트 **식별자로 조회** |
| 사용 주체 | **응용 서비스** |

## 리포지터리란

도메인 객체를 지속적으로 사용하려면 **RDBMS, NoSQL, 로컬 파일** 같은 물리적인 저장소에 도메인 객체를 보관해야 한다. 이를 위한 도메인 모델이 **리포지터리(Repository)** 다.

> [!important] 위상의 차이
> - 엔티티나 밸류 = **요구사항에서 도출되는** 도메인 모델
> - 리포지터리 = **구현을 위한** 도메인 모델

## 애그리거트 단위로 저장하고 조회한다

```java
public interface OrderRepository {
    Order findByNumber(OrderNumber number);
    void save(Order order);
    void delete(Order order);
}
```

> [!tip]
> 메서드를 보면 대상을 찾고 저장하는 **단위가 애그리거트 루트인 `Order`** 인 것을 알 수 있다.
> `Order`는 애그리거트에 속한 모든 객체를 포함하고 있으므로, 결과적으로 **애그리거트 단위로 저장하고 조회**한다.

## 사용 흐름 — 응용 서비스가 사용한다

```java
public class CancelOrderService {
    private OrderRepository orderRepository;

    @Transactional                       // 응용 서비스는 트랜잭션을 관리한다
    public void cancel(OrderNumber number) {
        Order order = orderRepository.findByNumber(number);
        if (order == null) throw new NoOrderException(number);
        order.cancel();
    }
}
```

도메인 모델을 사용해야 하는 코드는 **리포지터리를 통해서 도메인 객체를 구한 뒤에 도메인 객체의 기능을 실행**한다.

## DIP 적용 구조 (그림 2.19)

```
   [ 응용 영역 ]
     CancelOrderService
             │
   ─ ─ ─ ─ ─ ┼ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
   [ 도메인 영역 ]        │
     «Entity» Order    «interface» OrderRepository   ◀── 고수준 모듈
                                    △
   ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ╎ ─ ─ ─ ─ ─ ─
   [ 인프라스트럭처 영역 ]           ╎
                        JpaOrderRepository          ◀── 저수준 모듈
```

> [!important] DIP의 대표적 적용 사례
> - 도메인 모델 관점에서 `OrderRepository`는 도메인 객체를 **영속화하는 데 필요한 기능을 추상화한 것**으로 **고수준 모듈**에 속한다.
> - 기반 기술(JPA/MyBatis 등)을 이용해서 `OrderRepository`를 구현한 클래스는 **저수준 모듈**로 **인프라스트럭처 영역**에 속한다.
>
> 응용 서비스는 **의존 주입(DI)** 과 같은 방식을 사용해서 실제 리포지터리 구현 객체에 접근한다.

## 응용 서비스와 리포지터리의 밀접한 관계

> [!important] 왜 밀접한가 — 두 가지 이유
> ① 응용 서비스는 필요한 **도메인 객체를 구하거나 저장할 때 리포지터리를 사용**한다.
> ② 응용 서비스는 **트랜잭션을 관리**하는데, **트랜잭션 처리는 리포지터리 구현 기술의 영향을 받는다.**

리포지터리를 사용하는 주체가 응용 서비스이기 때문에, 리포지터리는 **응용 서비스가 필요로 하는 메서드를 제공**한다.

### 기본이 되는 두 메서드

```java
public interface SomeRepository {
    void save(Some some);            // ① 애그리거트를 저장하는 메서드
    Some findById(SomeId id);        // ② 애그리거트 루트 식별자로 조회하는 메서드
}
```

이 외에 필요에 따라 `delete(id)`나 `counts()` 등의 메서드를 제공하기도 한다.

> [!note] 구현 방법
> 리포지터리를 구현하는 방법은 **선택한 구현 기술에 따라 달라진다.**
> 자세한 내용은 4장 [[JPA 리포지터리 구현]] 참고.

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "리포지터리의 저장/조회 단위" | **애그리거트(루트)** |
| "리포지터리 인터페이스는 어느 영역?" | **도메인 모델 영역** (고수준) |
| "리포지터리 구현 클래스는 어느 영역?" | **인프라스트럭처 영역** (저수준) |
| "리포지터리의 기본 메서드 2가지" | **`save()`** + **식별자로 조회(`findById`)** |
| "리포지터리를 사용하는 주체" | **응용 서비스** |
| "응용 서비스와 리포지터리가 밀접한 두 이유" | 도메인 객체 조회/저장 + **트랜잭션 처리가 구현 기술 영향을 받음** |
| "엔티티와 리포지터리의 도출 차이" | 엔티티=**요구사항에서 도출** / 리포지터리=**구현을 위한 모델** |
| "응용 서비스는 구현 객체를 어떻게 얻나?" | **의존 주입(DI)** |

## Related Notes

- [[도메인 영역의 주요 구성요소]] — 5대 구성요소
- [[DIP]] — 고수준/저수준과 인터페이스 위치
- [[DIP와 아키텍처]]
- [[애그리거트 개요]] — 저장 단위
- [[리포지터리와 애그리거트]] — 3장 심화
- [[JPA 리포지터리 구현]] — 4장 구현
- [[요청 처리 흐름]]
- [[연습문제 - 아키텍처 개요]]
