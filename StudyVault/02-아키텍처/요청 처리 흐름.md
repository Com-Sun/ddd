---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 2장 §2.5 (p.090~091)
keywords: request-flow, controller, application-service, transaction
---

# 요청 처리 흐름 (★★)

#ddd #architecture #request-flow #concept

## Overview Table (한눈에 비교)

| 단계 | 영역 | 하는 일 |
|------|------|---------|
| 1 | **표현** | 요청을 처음 받는다. 데이터 **형식이 올바른지 검사** |
| 2 | **표현** | 사용자 데이터를 **응용 서비스가 요구하는 형식으로 변환**해 전달 |
| 3 | **응용** | 도메인 모델을 이용해 **기능 구현**, **트랜잭션 관리** |
| 4 | **도메인** | 실제 도메인 로직 수행 |
| 5 | **인프라** | 리포지터리를 통해 저장/조회 |
| 6 | **표현** | 결과를 **HTTP 응답으로 변환** |

## 전체 흐름 (그림 2.20)

```
 브라우저      Controller      ApplicationService      Domain        Repository
    │              │                    │                 │              │
    │─HTTP 요청──▶ │                    │                 │              │
    │              │ 1.1 요청 데이터를  │                 │              │
    │              │ 응용 서비스에 맞게 │                 │              │
    │              │ 변환               │                 │              │
    │              │─1.2 기능 실행─────▶│                 │              │
    │              │                    │─1.2.1 find()───────────────────▶│
    │              │                    │◀─1.2.2 도메인 객체 리턴─────────│
    │              │                    │─1.2.3 도메인 기능 실행─▶│      │
    │              │◀─1.2.4 리턴────────│                 │              │
    │◀─1.2.5 HTTP 응답─│                 │                 │              │
```

## 각 단계 상세

### 표현 영역

- 사용자가 애플리케이션에 기능 실행을 요청하면 **그 요청을 처음 받는 영역은 표현 영역**이다.
- 스프링 MVC로 웹 애플리케이션을 구현했다면 **컨트롤러**가 사용자의 요청을 받아 처리한다.
- 표현 영역은 사용자가 전송한 **데이터 형식이 올바른지 검사**하고, 문제가 없다면 데이터를 이용해서 **응용 서비스에 기능 실행을 위임**한다.
- 이때 사용자가 전송한 데이터를 **응용 서비스가 요구하는 형식으로 변환**해서 전달한다.

### 응용 서비스

응용 서비스는 도메인 모델을 이용해서 기능을 구현한다.

| 패턴 | 설명 |
|------|------|
| 조회 후 실행 | 기능 구현에 필요한 **도메인 객체를 리포지터리에서 가져와 실행** |
| 생성 후 저장 | **신규 도메인 객체를 생성해서 리포지터리에 저장** |
| 복합 | **두 개 이상의 도메인 객체**를 사용해서 구현하기도 한다 |

### 트랜잭션 관리 — 응용 서비스의 책임

> [!important]
> '예매하기'나 '예매 취소'와 같은 기능을 제공하는 응용 서비스는 **도메인의 상태를 변경**하므로,
> 변경 상태가 **물리 저장소에 올바르게 반영되도록 트랜잭션을 관리**해야 한다.

```java
public class CancelOrderService {
    private OrderRepository orderRepository;

    @Transactional            // ★ 응용 서비스는 트랜잭션을 관리한다
    public void cancel(OrderNumber number) {
        Order order = orderRepository.findByNumber(number);
        if (order == null) throw new NoOrderException(number);
        order.cancel();
    }
}
```

스프링 프레임워크를 사용하면 스프링이 제공하는 **`@Transactional` 애너테이션**으로 트랜잭션을 처리할 수 있다.

> [!note]
> 응용 서비스 구현에 대한 자세한 내용은 6장 [[응용 서비스의 역할]] 참고.

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "요청을 처음 받는 영역" | **표현 영역** |
| "표현 영역이 하는 검사" | 전송 데이터의 **형식이 올바른지** |
| "표현 영역이 하는 변환" | 사용자 데이터 → **응용 서비스가 요구하는 형식** |
| "트랜잭션은 누가 관리?" | **응용 서비스** |
| "트랜잭션 관리가 필요한 이유" | 변경 상태가 **물리 저장소에 올바르게 반영**되도록 |
| "스프링에서 트랜잭션 처리" | **`@Transactional`** |
| "응용 서비스의 세 구현 패턴" | 리포지터리 조회 후 실행 / 신규 생성 후 저장 / **두 개 이상 도메인 객체 사용** |

## Related Notes

- [[네 개의 영역]] — 각 영역의 책임
- [[리포지터리 개요]] — 조회/저장
- [[표현 영역과 응용 영역]] — 6장 심화
- [[표현 영역에 의존하지 않기와 트랜잭션 처리]] — 6장의 트랜잭션 상세
- [[값 검증]] — 표현/응용 영역의 검증 분담
- [[연습문제 - 아키텍처 개요]]
