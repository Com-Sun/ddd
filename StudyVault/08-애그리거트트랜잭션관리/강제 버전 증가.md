---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 8장 §8.3.1 (p.261~262)
keywords: forced-increment, version, aggregate-consistency, lockmodetype
---

# 강제 버전 증가 (★★★)

#ddd #transaction #optimistic-lock #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 문제 | 루트가 **아닌** 다른 엔티티의 값만 변경되면 **JPA는 루트의 버전을 증가시키지 않는다** |
| JPA의 논리 | 연관 엔티티가 바뀌어도 **루트 엔티티 자체의 값은 바뀐 것이 없으므로** 갱신하지 않는다 |
| ★ 애그리거트 관점 | **구성요소 중 일부 값이 바뀌면 논리적으로 그 애그리거트는 바뀐 것** |
| 필요 | 어떤 구성요소의 상태가 바뀌면 **루트의 버전 값이 증가해야 비선점 잠금이 올바르게 동작** |
| 해법 | **`LockModeType.OPTIMISTIC_FORCE_INCREMENT`** |
| 스프링 데이터 JPA | **`@Lock`** 애너테이션으로 지정 |

## 문제

> 애그리거트에 애그리거트 루트 외에 다른 엔티티가 존재하는데, **기능 실행 도중 루트가 아닌 다른 엔티티의 값만 변경**된다고 하자.

```
   [ 애그리거트 ]
   Order (루트)  version = 5      ← 값이 안 바뀜 → JPA는 version 그대로
     └─ OrderLine (엔티티)         ← 이것만 변경됨
                    ▼
   DB: version = 5  (증가 안 함)
                    ▼
   ❌ 다른 트랜잭션이 같은 애그리거트를 수정해도 충돌을 감지하지 못한다
```

> [!important] JPA의 동작
> **연관된 엔티티의 값이 변경된다고 해도 루트 엔티티 자체의 값은 바뀌는 것이 없으므로
> 루트 엔티티의 버전 값은 갱신하지 않는다.**

> [!danger] 애그리거트 관점에서는 문제다
> **비록 루트 엔티티의 값이 바뀌지 않았더라도, 애그리거트의 구성요소 중 일부 값이 바뀌면 논리적으로 그 애그리거트는 바뀐 것이다.**
> 따라서 **애그리거트 내에 어떤 구성요소의 상태가 바뀌면 루트 애그리거트의 버전 값이 증가해야 비선점 잠금이 올바르게 동작**한다.

## 해법 — `OPTIMISTIC_FORCE_INCREMENT`

> JPA는 이런 문제를 처리할 수 있도록 **엔티티를 구할 때 강제로 버전 값을 증가시키는 잠금 모드**를 지원한다.

```java
@Repository
public class JpaOrderRepository implements OrderRepository {
    @PersistenceContext
    private EntityManager entityManager;

    @Override
    public Order findByIdOptimisticLockMode(OrderNo id) {
        return entityManager.find(
                Order.class, id, LockModeType.OPTIMISTIC_FORCE_INCREMENT);
    }
}
```

> [!important]
> **`LockModeType.OPTIMISTIC_FORCE_INCREMENT`를 사용하면
> 해당 엔티티의 상태가 변경되었는지에 상관없이 트랜잭션 종료 시점에 버전 값 증가 처리를 한다.**
>
> 이 잠금 모드를 사용하면 **애그리거트 루트 엔티티가 아닌 다른 엔티티나 밸류가 변경되더라도 버전 값을 증가**시킬 수 있으므로,
> **비선점 잠금 기능을 안전하게 적용**할 수 있다.

```
   LockModeType.OPTIMISTIC_FORCE_INCREMENT
             ▼
   상태 변경 여부와 무관하게 트랜잭션 종료 시점에 version + 1
             ▼
   ⭕ 애그리거트 전체의 변경을 루트 버전이 대표하게 된다
```

### 스프링 데이터 JPA

> 스프링 데이터 JPA를 사용하면 앞서 살펴본 **`@Lock` 애너테이션**을 이용해서 지정하면 된다.

```java
public interface OrderRepository extends Repository<Order, OrderNo> {

    @Lock(LockModeType.OPTIMISTIC_FORCE_INCREMENT)
    @Query("select o from Order o where o.number = :orderNo")
    Optional<Order> findByIdOptimisticLockMode(@Param("orderNo") OrderNo orderNo);
}
```

## 잠금 모드 비교

| `LockModeType` | 용도 |
|----------------|------|
| **`PESSIMISTIC_WRITE`** | **선점 잠금** (`for update`) |
| **`OPTIMISTIC`** | 비선점 잠금 (버전 확인) |
| **`OPTIMISTIC_FORCE_INCREMENT`** | **비선점 + 강제 버전 증가** |

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "루트가 아닌 엔티티만 변경되면?" | JPA는 **루트의 버전을 증가시키지 않는다** |
| "JPA가 그렇게 동작하는 이유" | **루트 엔티티 자체의 값이 바뀌지 않았으므로** |
| "애그리거트 관점에서의 판단" | 구성요소가 바뀌면 **논리적으로 애그리거트가 바뀐 것** |
| "해결 잠금 모드" | **`OPTIMISTIC_FORCE_INCREMENT`** |
| "이 모드의 동작" | **상태 변경 여부와 무관하게** 트랜잭션 종료 시점에 버전 증가 |
| "스프링 데이터 JPA 지정법" | **`@Lock`** |

## Related Notes

- [[비선점 잠금]] — 기본 원리
- [[선점 잠금]] — `PESSIMISTIC_WRITE`
- [[애그리거트와 트랜잭션]]
- [[애그리거트 루트]] — 애그리거트의 일관성
- [[리포지터리와 애그리거트]] — 애그리거트는 개념적으로 하나
- [[연습문제 - 애그리거트 트랜잭션 관리]]
