---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 10장 §10.2~10.2.4 (p.304~310)
keywords: event, dispatcher, handler, past-tense, trigger, synchronization
---

# 이벤트 개요 (★★★)

#ddd #event #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 이벤트의 뜻 | **과거에 벌어진 어떤 것** |
| 이벤트 발생 = | **상태가 변경됐다**는 의미 |
| **네 가지 구성요소** | **이벤트 / 이벤트 생성 주체 / 이벤트 디스패처(퍼블리셔) / 이벤트 핸들러(구독자)** |
| 생성 주체 | **엔티티, 밸류, 도메인 서비스** 같은 도메인 객체 |
| 이벤트 이름 | **과거 시제**를 사용 (`ShippingInfoChangedEvent`) |
| 이벤트가 담는 것 | **이벤트 종류(클래스 이름)** + **추가 데이터** |
| **용도 ①** | **트리거(trigger)** — 후처리 실행 |
| **용도 ②** | **서로 다른 시스템 간의 데이터 동기화** |
| **장점 ①** | 서로 다른 도메인 로직이 **섞이는 것을 방지** |
| **장점 ②** | **기능 확장이 용이** — 핸들러만 추가하면 된다 |

## 이벤트란

> 이 절에서 사용하는 **'이벤트'라는 용어는 '과거에 벌어진 어떤 것'을 의미**한다.
> 사용자가 암호를 변경했다면 **'암호를 변경했음 이벤트'** 가 벌어졌다고 할 수 있다.

> [!important]
> **이벤트가 발생한다는 것은 상태가 변경됐다는 것을 의미**한다.

> [!tip] UI 이벤트와의 유사성
> ```javascript
> $("#myBtn").click(function(evt) {
>     alert("경고");
> });
> ```
> 이벤트는 **발생하는 것에서 끝나지 않는다.**
> **이벤트가 발생하면 그 이벤트에 반응하여 원하는 동작을 수행하는 기능을 구현**한다.

> 도메인 모델에서도 도메인의 상태 변경과 관련된 요구사항이 많다.
> **"주문을 취소할 때 이메일을 보낸다"** 에서 '주문을 취소할 때'는 **'주문 취소됨 이벤트'** 로 구현할 수 있다.

## §10.2.1 이벤트 관련 구성요소

```
   ┌──────────────────┐   이벤트 생성   ┌──────────────────┐
   │ 이벤트 생성 주체   │ ─────────────▶ │ 이벤트 디스패처   │
   │ (엔티티/밸류/     │                │ (이벤트 퍼블리셔) │
   │  도메인 서비스)   │                └──────────────────┘
   └──────────────────┘                          │ 이벤트 전파
                                                  ▼
                                        ┌──────────────────┐
                                        │  이벤트 핸들러    │
                                        │   (구독자)       │
                                        └──────────────────┘
```

| 구성요소 | 역할 |
|----------|------|
| **이벤트 생성 주체** | **엔티티, 밸류, 도메인 서비스** 같은 도메인 객체. **도메인 로직을 실행해서 상태가 바뀌면 관련 이벤트를 발생**시킨다 |
| **이벤트 핸들러(구독자)** | 생성 주체가 발생한 **이벤트에 반응**한다. 이벤트에 담긴 데이터로 원하는 기능을 실행 |
| **이벤트 디스패처(퍼블리셔)** | **생성 주체와 핸들러를 연결**한다. 전달받은 이벤트를 **처리할 수 있는 핸들러에 전파** |

> [!important]
> **이벤트 디스패처의 구현 방식에 따라 이벤트 생성과 처리를 동기나 비동기로 실행**하게 된다.

## §10.2.2 이벤트의 구성

> 이벤트는 발생한 이벤트에 대한 정보를 담는다.

| 정보 | 설명 |
|------|------|
| **이벤트 종류** | **클래스 이름**으로 이벤트 종류를 표현 |
| **추가 데이터** | 주문번호, 신규 배송지 정보 등 **이벤트와 관련된 정보** |

```java
public class ShippingInfoChangedEvent {
    private String orderNumber;
    private long timestamp;
    private ShippingInfo newShippingInfo;

    // 생성자, getter
}
```

> [!important] 과거 시제를 쓰는 이유
> 클래스 이름을 보면 **`Changed`라는 과거 시제**를 사용했다.
> **이벤트는 현재 기준으로 과거(바로 직전이라도)에 벌어진 것을 표현하기 때문에 이벤트 이름에는 과거 시제를 사용**한다.

### 발생 주체

```java
public class Order {

    public void changeShippingInfo(ShippingInfo newShippingInfo) {
        verifyNotYetShipped();
        setShippingInfo(newShippingInfo);
        Events.raise(new ShippingInfoChangedEvent(number, newShippingInfo));
    }
}
```

### 핸들러

```java
public class ShippingInfoChangedHandler {

    @EventListener(ShippingInfoChangedEvent.class)
    public void handle(ShippingInfoChangedEvent evt) {
        shippingInfoSynchronizer.sync(
                evt.getOrderNumber(),
                evt.getNewShippingInfo());
    }
}
```

## ★ 이벤트가 담아야 할 데이터의 양

> [!important] 원칙 ① — 부족하면 안 된다
> **이벤트는 이벤트 핸들러가 작업을 수행하는 데 필요한 데이터를 담아야 한다.**
> 데이터가 부족하면 핸들러는 **관련 API를 호출하거나 DB에서 데이터를 직접 읽어와야** 한다.

```java
public class ShippingInfoChangedHandler {

    @EventListener(ShippingInfoChangedEvent.class)
    public void handle(ShippingInfoChangedEvent evt) {
        // 이벤트가 필요한 데이터를 담고 있지 않으면,
        // 이벤트 핸들러는 리포지터리, 조회 API, 직접 DB 접근 등의
        // 방식을 통해 필요한 데이터를 조회해야 한다.
        Order order = orderRepository.findById(evt.getOrderNo());
        shippingInfoSynchronizer.sync(
                order.getNumber().getValue(),
                order.getShippingInfo());
    }
}
```

> [!warning] 원칙 ② — 과해도 안 된다
> **이벤트는 데이터를 담아야 하지만 그렇다고 이벤트 자체와 관련 없는 데이터를 포함할 필요는 없다.**
> 배송지 정보 변경으로 발생한 `ShippingInfoChangedEvent`가 **바뀐 배송지 정보를 포함하는 것은 맞지만**,
> **배송지 정보 변경과 전혀 관련 없는 주문 상품 번호와 개수를 담을 필요는 없다.**

## §10.2.3 이벤트 용도

| 용도 | 설명 | 예 |
|------|------|-----|
| **① 트리거(trigger)** | 도메인의 상태가 바뀔 때 **다른 후처리가 필요하면 후처리를 실행하기 위한 트리거** | 주문 취소 → **환불 처리** / 예매 완료 → **SMS 통지** |
| **② 데이터 동기화** | **서로 다른 시스템 간의 데이터 동기화** | 배송지 변경 → **외부 배송 서비스와 배송지 정보 동기화** |

```
   [ 트리거 ]
   Order ──OrderCanceledEvent──▶ 이벤트 핸들러 ──▶ RefundService

   [ 데이터 동기화 ]
   Order ──ShippingInfoChangedEvent──▶ 핸들러 ──▶ 외부 배송 서비스
```

## §10.2.4 이벤트 장점

### 장점 ① — 도메인 로직이 섞이지 않는다

```java
// ❌ Before
public class Order {
    public void cancel(RefundService refundService) {
        verifyNotYetShipped();
        this.state = OrderState.CANCELED;
        this.refundStatus = State.REFUND_STARTED;
        try {
            refundService.refund(getPaymentId());     // 결제 로직이 섞임
            this.refundStatus = State.REFUND_COMPLETED;
        } catch (Exception ex) { }
    }
}

// ⭕ After — 이벤트로 서로 다른 도메인 로직이 섞이는 것 방지
public class Order {
    public void cancel() {
        verifyNotYetShipped();
        this.state = OrderState.CANCELED;
        Events.raise(new OrderCanceledEvent(number.getNumber()));
    }
}
```

> [!important]
> 구매 취소 로직에 이벤트를 적용함으로써 **환불 로직이 없어졌다.**
> `cancel()` 메서드에서 **환불 서비스를 실행하기 위해 사용한 파라미터도 없어졌다.**
> 환불 실행 로직은 **주문 취소 이벤트를 받는 이벤트 핸들러로 이동**하게 된다.
> → **이벤트를 사용하여 주문 도메인에서 결제(환불) 도메인으로의 의존을 제거했다.**

### 장점 ② — 기능 확장이 용이하다

```
   OrderCanceledEvent
        ├──▶ OrderCanceledEventHandler  ──▶ 환불
        └──▶ OrderCanceledEventHandler2 ──▶ 이메일 통지   ← 핸들러만 추가!
                                              ▲
                             구매 취소 로직은 수정할 필요가 없다
```

> [!tip]
> 구매 취소 시 **환불과 함께 이메일로 취소 내용을 보내고 싶다면 이메일 발송을 처리하는 핸들러를 구현하면 된다.**
> **기능을 확장해도 구매 취소 로직은 수정할 필요가 없다.**

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "이벤트의 뜻" | **과거에 벌어진 어떤 것** |
| "이벤트 발생 = ?" | **상태가 변경됐다** |
| "네 가지 구성요소" | **이벤트 / 생성 주체 / 디스패처(퍼블리셔) / 핸들러(구독자)** |
| "생성 주체가 될 수 있는 것" | **엔티티, 밸류, 도메인 서비스** |
| "동기/비동기를 결정하는 것" | **이벤트 디스패처의 구현 방식** |
| "이벤트 이름의 시제" | **과거 시제** |
| "이벤트가 담는 정보 2가지" | **이벤트 종류(클래스 이름) + 추가 데이터** |
| "데이터가 부족하면?" | 핸들러가 **API 호출/DB 직접 조회**를 해야 한다 |
| "관련 없는 데이터를 넣어도 되나?" | **넣을 필요 없다** |
| "이벤트의 두 용도" | **트리거 / 시스템 간 데이터 동기화** |
| "이벤트의 두 장점" | **로직 섞임 방지 / 기능 확장 용이** |

## Related Notes

- [[시스템 간 강결합 문제]] — 이벤트가 해결하는 문제
- [[이벤트 핸들러와 디스패처 구현]] — 실제 구현
- [[동기 이벤트 처리 문제]]
- [[비동기 이벤트 처리]]
- [[트랜잭션 범위]] — 3장이 예고한 해법
- [[연습문제 - 이벤트]]
