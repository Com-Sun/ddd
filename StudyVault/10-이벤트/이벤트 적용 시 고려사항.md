---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 10장 §10.6~10.6.1 (p.338~341)
keywords: idempotent, retry, event-loss, ordering, transactionaleventlistener
---

# 이벤트 적용 시 추가 고려 사항 (★★★)

#ddd #event #transaction #concept

## Overview Table (다섯 가지 고려 사항)

| # | 고려 사항 | 핵심 |
|---|-----------|------|
| **①** | **이벤트 소스를 `EventEntry`에 추가할지** | 현재는 **발생 주체 정보가 없어** "특정 주체가 발생시킨 이벤트만 조회"가 불가능 |
| **②** | **전송 실패를 얼마나 허용할 것인가** | **재전송 횟수 제한**을 두지 않으면 **한 이벤트가 전체를 막는다** |
| **③** | **이벤트 손실** | 이벤트 저장소 = **손실 없음** / 로컬 핸들러 비동기 = **유실 가능** |
| **④** | **이벤트 순서** | 순서가 중요하면 **이벤트 저장소** 사용 |
| **⑤** | **이벤트 재처리** | **마지막 처리 순번 기억** 또는 **멱등성(idempotent)** |
| **DB 트랜잭션** | **`@TransactionalEventListener`** 로 트랜잭션 성공 시에만 핸들러 실행 |

## ① 이벤트 소스를 `EventEntry`에 추가할지

> [!warning]
> 앞서 구현한 `EventEntry`는 **이벤트 발생 주체에 대한 정보를 갖지 않는다.**
> 따라서 **"`Order`가 발생시킨 이벤트만 조회하기"** 처럼 **특정 주체가 발생시킨 이벤트만 조회하는 기능을 구현할 수 없다.**
> 이 기능을 구현하려면 **이벤트에 발생 주체 정보를 추가**해야 한다.

## ② 포워더의 전송 실패 허용 횟수

> [!danger] 한 이벤트가 전체를 막는다
> 포워더는 이벤트 전송에 실패하면 **실패한 이벤트부터 다시 읽어와 전송을 시도**한다.
> **그런데 특정 이벤트에서 계속 전송에 실패하면 그 이벤트 때문에 나머지 이벤트를 전송할 수 없게 된다.**

```
   [이벤트 A] ← 계속 실패
   [이벤트 B]  ┐
   [이벤트 C]  ├─ 영원히 전송되지 않음
   [이벤트 D]  ┘
```

> [!important] 해법
> 포워더를 구현할 때는 **실패한 이벤트의 재전송 횟수 제한을 두어야 한다.**
> 예: **동일 이벤트를 전송하는 데 3회 실패했다면 해당 이벤트는 생략하고 다음 이벤트로 넘어간다**는 등의 정책이 필요하다.

> [!tip] 실패 보관
> 처리에 실패한 이벤트를 **생략하지 않고 별도 실패용 DB나 메시지 큐에 저장**하기도 한다.
> **물리적인 저장소에 남겨두면 이후 실패 이유 분석이나 후처리에 도움**이 된다.

## ③ 이벤트 손실

| 방식 | 이벤트 손실 |
|------|-------------|
| **이벤트 저장소 사용** | **발생과 저장을 한 트랜잭션**으로 처리 → 트랜잭션 성공 시 **저장소에 보관됨을 보장** ⭕ |
| **로컬 핸들러 비동기 처리** | 이벤트 처리에 실패하면 **이벤트를 유실** ❌ |

## ④ 이벤트 순서

> [!important]
> **이벤트 발생 순서대로 외부 시스템에 전달해야 할 경우, 이벤트 저장소를 사용하는 것이 좋다.**
> 이벤트 저장소는 **저장소에 이벤트를 발생 순서대로 저장하고 그 순서대로 이벤트 목록을 제공**하기 때문이다.
>
> 반면에 **메시징 시스템은 사용 기술에 따라 이벤트 발생 순서와 메시지 전달 순서가 다를 수도 있다.**

## ⑤ 이벤트 재처리

> **동일한 이벤트를 다시 처리해야 할 때 이벤트를 어떻게 할지 결정해야 한다.**

| 방법 | 설명 |
|------|------|
| **① 순번 기억** | **마지막으로 처리한 이벤트의 순번을 기억**해 두었다가, 이미 처리한 순번의 이벤트가 도착하면 **처리하지 않고 무시** |
| **② 멱등성(idempotent)** | **동일 이벤트를 한 번 적용하나 여러 번 적용하나 시스템이 같은 상태**가 되도록 핸들러를 구현 |

> 예: 회원 가입 신청 이벤트가 처음 도착하면 이메일을 발송하는데, **동일한 순번의 이벤트가 다시 들어오면 이메일을 발송하지 않는** 방식.

### 멱등성(Idempotent)

> [!important]
> **연산을 여러 번 적용해도 결과가 달라지지 않는 성질을 멱등성**이라고 한다.
> 수학에서 **절댓값 함수 `abs()`** 가 대표적이다. `abs(abs(x)) == abs(x)`
>
> **이벤트 처리도 동일 이벤트를 한 번 적용하나 여러 번 적용하나 시스템이 같은 상태가 되도록 핸들러를 구현할 수 있다.**
> 예: **배송지 정보 변경 이벤트**를 받아 주소를 변경하는 핸들러는 **한 번 처리하나 여러 번 처리하나 결과적으로 동일 주소**를 값으로 갖는다.

> [!tip]
> 핸들러가 **멱등성을 가지면 시스템 장애로 인해 같은 이벤트가 중복해서 발생해도 결과적으로 동일 상태**가 된다.
> 이는 **이벤트 중복 발생이나 중복 처리에 대한 부담을 줄여준다.**

## ★ §10.6.1 이벤트 처리와 DB 트랜잭션 고려

> 주문 취소와 환불 기능을 이벤트로 구현했다고 하자.
> - 주문 취소 기능은 **주문 취소 이벤트를 발생**시킨다.
> - 주문 취소 이벤트 핸들러는 **환불 서비스에 환불 처리를 요청**한다.
> - 환불 서비스는 **외부 API를 호출**해서 결제를 취소한다.

### 경우의 수가 폭발한다

```
   트랜잭션 성공 + 이벤트 처리 성공   ⭕
   트랜잭션 성공 + 이벤트 처리 실패   → 환불 재처리 필요
   트랜잭션 실패 + 이벤트 처리 성공   ❌ 주문은 취소 안 됐는데 환불됨!
   트랜잭션 실패 + 이벤트 처리 실패   → ?
```

> [!important] 해법 — 경우의 수를 줄인다
> **트랜잭션 실패와 이벤트 처리 실패를 모두 고려하면 복잡하므로 경우의 수를 줄이면 도움이 된다.**
> 경우의 수를 줄이는 방법은 **트랜잭션이 성공할 때만 이벤트 핸들러를 실행하는 것**이다.

### `@TransactionalEventListener`

> **스프링은 `@TransactionalEventListener` 애너테이션을 지원한다.**
> 이 애너테이션은 **스프링 트랜잭션 상태에 따라 이벤트 핸들러를 실행**할 수 있게 한다.

```java
@TransactionalEventListener(
    classes = OrderCanceledEvent.class,
    phase = TransactionPhase.AFTER_COMMIT
)
public void handle(OrderCanceledEvent event) {
    refundService.refund(event.getOrderNumber());
}
```

> [!important]
> `phase` 속성 값으로 **`TransactionPhase.AFTER_COMMIT`** 을 지정하면
> 스프링은 **트랜잭션 커밋에 성공한 뒤에 핸들러 메서드를 실행**한다.
> **중간에 에러가 발생해서 트랜잭션이 롤백 되면 핸들러 메서드를 실행하지 않는다.**
>
> → **핸들러를 실행했는데 트랜잭션이 롤백 되는 상황은 발생하지 않는다.**

```
   [트랜잭션 시작] → 도메인 기능 → 이벤트 발생
                          │
                    커밋 성공? ──아니오──▶ 핸들러 실행 안 함  ⭕
                          │ 예
                          ▼
                    핸들러 실행
```

### 이벤트 저장소로 DB를 써도 같은 효과

> [!tip]
> **이벤트 저장소로 DB를 사용해도 동일한 효과를 볼 수 있다.**
> **이벤트 발생 코드와 이벤트 저장 처리를 한 트랜잭션으로 처리**하면 된다.
> 이렇게 하면 **트랜잭션이 성공할 때만 이벤트가 DB에 저장되므로,
> 트랜잭션은 실패했는데 이벤트 핸들러가 실행되는 상황은 발생하지 않는다.**

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "다섯 가지 고려 사항" | 이벤트 소스 / **재전송 제한** / **손실** / **순서** / **재처리** |
| "특정 주체의 이벤트만 조회하려면" | **`EventEntry`에 발생 주체 정보 추가** |
| "재전송 제한이 없으면?" | **한 이벤트가 나머지 전체를 막는다** |
| "실패한 이벤트 처리 팁" | **별도 실패용 DB/큐에 저장** → 분석·후처리 |
| "이벤트 손실이 없는 방식" | **이벤트 저장소** |
| "이벤트 손실이 있는 방식" | **로컬 핸들러 비동기** |
| "순서 보장이 필요하면?" | **이벤트 저장소** (메시징은 순서가 다를 수 있다) |
| "재처리 두 방법" | **마지막 순번 기억 / 멱등성** |
| "멱등성의 정의" | **여러 번 적용해도 결과가 달라지지 않는 성질** |
| "멱등성의 수학 예" | **절댓값 함수 `abs()`** |
| "경우의 수를 줄이는 방법" | **트랜잭션 성공 시에만 핸들러 실행** |
| "그 애너테이션과 phase" | **`@TransactionalEventListener`** + **`AFTER_COMMIT`** |
| "DB 이벤트 저장소로도 가능한 이유" | 이벤트 발생과 저장을 **한 트랜잭션**으로 처리 |

## Related Notes

- [[이벤트 저장소를 이용한 비동기 처리]] — 포워더·API 구현
- [[비동기 이벤트 처리]] — 네 가지 방식
- [[메시징 시스템을 이용한 비동기 구현]] — 순서 보장 한계
- [[동기 이벤트 처리 문제]]
- [[이벤트 개요]]
- [[표현 영역에 의존하지 않기와 트랜잭션 처리]] — 6장
- [[연습문제 - 이벤트]]
