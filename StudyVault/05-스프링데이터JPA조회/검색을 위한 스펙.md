---
source_pdf: 도메인 주도 개발 시작하기.pdf
part: 5장 §5.1~5.2 (p.174~177)
keywords: specification, query-model, dao, search-condition
---

# 검색을 위한 스펙 (★★★)

#ddd #query #specification #concept

## Overview Table (한눈에 비교)

| 항목 | 핵심 내용 |
|------|-----------|
| 이 장의 위상 | **조회 모델**을 구현하는 방법 (5장 전체) |
| 도메인 모델의 위상 | 주로 **명령 모델**(상태 변경)로 사용 |
| 용어 혼용 | **리포지터리**(도메인 모델에 속한) ↔ **DAO**(데이터 접근) — 이 장에서 혼용 |
| 문제 | 검색 조건 조합이 늘수록 **`find` 메서드도 함께 증가** |
| 해법 | **스펙(Specification)** — 애그리거트가 **특정 조건을 충족하는지 검사**하는 인터페이스 |
| 인터페이스 | `boolean isSatisfiedBy(T agg)` |
| `agg`의 정체 | 리포지터리에 쓰면 **애그리거트 루트**, DAO에 쓰면 **검색 결과로 리턴할 데이터 객체** |
| ★ 주의 | 이 절의 순수 구현은 **실제로는 이렇게 구현하지 않는다** |

## §5.1 시작에 앞서 — 명령 모델과 조회 모델

> [!important] CQRS 예고
> **명령 모델**은 **상태를 변경**하는 기능을 구현할 때 사용하고, **조회 모델**은 **데이터를 조회**하는 기능을 구현할 때 사용한다.

| 모델 | 예시 기능 | 이 책에서 다룬 곳 |
|------|-----------|-------------------|
| **명령 모델** | 회원 가입, 암호 변경, 주문 취소, 배송지 변경 | 엔티티, 애그리거트, 리포지터리 (1~4장) |
| **조회 모델** | 주문 목록, 주문 상세, 상품 상세 — **정렬·페이징·검색 조건 지정** | **5장** |

> [!note] 용어 혼용에 대하여
> 이러한 이유로 이 장의 예제 코드는 **리포지터리**(도메인 모델에 속한)와 **DAO**(데이터 접근을 의미하는)라는 이름을 **혼용**한다.

> [!tip] 기술 선택은 자유
> 저자는 조회 모델을 구현할 때 다양한 기술을 사용한다. **JPA, 마이바티스, JdbcTemplate** 등.
> **모든 DB 연동 코드를 JPA만 사용해서 구현해야 한다고 생각하지는 말자.**

## §5.2 스펙이 필요한 이유

검색 조건이 **고정되어 있고 단순하면** 특정 조건 메서드를 만들면 된다.

```java
public interface OrderDataDao {
    Optional<OrderData> findById(OrderNo id);
    List<OrderData> findByOrderer(String ordererId, Date fromDate, Date toDate);
    ...
}
```

> [!danger] 문제
> 목록 조회 같은 기능은 **다양한 검색 조건을 조합**해야 할 때가 있다.
> 필요한 조합마다 `find` 메서드를 정의할 수도 있지만 **좋은 방법이 아니다.**
> **조합이 증가할수록 정의해야 할 `find` 메서드도 함께 증가**하기 때문이다.

```
   조건 A, B, C가 있다면
   findByA, findByB, findByC,
   findByAB, findByAC, findByBC,
   findByABC ...                    ← 조합 폭발
```

## 스펙(Specification) 인터페이스

> [!important] 정의
> **스펙은 애그리거트가 특정 조건을 충족하는지를 검사할 때 사용하는 인터페이스**다.

```java
public interface Specification<T> {
    public boolean isSatisfiedBy(T agg);
}
```

> [!tip] `agg` 파라미터의 정체
> `isSatisfiedBy()`의 `agg` 파라미터는 **검사 대상이 되는 객체**다.
> - 스펙을 **리포지터리**에 사용하면 → `agg`는 **애그리거트 루트**
> - 스펙을 **DAO**에 사용하면 → `agg`는 **검색 결과로 리턴할 데이터 객체**
>
> `isSatisfiedBy()`는 검사 대상 객체가 조건을 **충족하면 `true`, 아니면 `false`** 를 리턴한다.

### 구현 예

```java
public class OrdererSpec implements Specification<Order> {
    private String ordererId;

    public OrdererSpec(String ordererId) {
        this.ordererId = ordererId;
    }

    @Override
    public boolean isSatisfiedBy(Order agg) {
        return agg.getOrderer().getMemberId().getId().equals(ordererId);
    }
}
```

### 리포지터리에서 사용 (개념적 구현)

```java
public class MemoryOrderRepository implements OrderRepository {

    public List<Order> findAll(Specification<Order> spec) {
        List<Order> allOrders = findAll();
        return allOrders.stream()
                .filter(order -> spec.isSatisfiedBy(order))
                .toList();
    }
}
```

```java
// 검색 조건을 표현하는 스펙을 생성해서
Specification<Order> ordererSpec = new OrdererSpec("madvirus");
// 리포지터리에 전달
List<Order> orders = orderRepository.findAll(ordererSpec);
```

## ★ 실제로는 이렇게 구현하지 않는다

> [!danger] 중요한 경고 (p.177)
> **하지만 실제 스펙은 이렇게 구현하지 않는다.**
> - **모든 애그리거트 객체를 메모리에 보관하기도 어렵고**
> - 설사 메모리에 다 보관할 수 있다 하더라도 **조회 성능에 심각한 문제가 발생**하기 때문이다.
>
> **실제 스펙은 사용하는 기술에 맞춰 구현하게 된다.**
> → [[스프링 데이터 JPA 스펙 구현]]

```
   [ 개념 설명용 구현 ]              [ 실제 구현 ]
   모든 애그리거트 로딩                DB 쿼리의 WHERE 절로 변환
        ▼                                   ▼
   메모리에서 filter                   DB가 필터링
        ▼                                   ▼
   ❌ 메모리 부족 / 성능 붕괴          ⭕ 실용적
```

## Exam/Test Patterns (시험 빈출 패턴)

| Scenario/Keyword | Answer |
|-------------------|--------|
| "상태 변경 기능 구현 모델" | **명령 모델** |
| "주문 목록·상세 조회 모델" | **조회 모델** |
| "검색 조건 조합이 늘면?" | **`find` 메서드가 함께 증가** → 나쁜 방법 |
| "스펙의 정의" | 애그리거트가 **특정 조건을 충족하는지 검사**하는 인터페이스 |
| "스펙의 메서드" | **`isSatisfiedBy(T agg)`** |
| "리포지터리에 쓸 때 `agg`는?" | **애그리거트 루트** |
| "DAO에 쓸 때 `agg`는?" | **검색 결과로 리턴할 데이터 객체** |
| "메모리 필터링 방식의 문제" | **메모리 보관 어려움 + 조회 성능 심각** |
| "조회를 JPA로만 해야 하나?" | **아니다.** 마이바티스·JdbcTemplate 등 자유 |

## Related Notes

- [[스프링 데이터 JPA 스펙 구현]] — 실제 구현
- [[스펙 조합]]
- [[단일 모델의 단점]] — 11장 CQRS의 문제의식
- [[CQRS]] — 명령/조회 모델 분리
- [[ID 참조와 조회 성능]] — 3장의 조회 전용 쿼리
- [[연습문제 - 스프링 데이터 JPA 조회 기능]]
